name: Deploy static site to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '17 */6 * * *'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Refresh latest YouTube videos
        run: node scripts/fetch-youtube-videos.mjs
        continue-on-error: true
      - name: Stamp asset version for cache-busting
        run: find dist -name '*.html' -print0 | xargs -0 sed -i "s/__ASSET_VERSION__/${GITHUB_SHA::8}-${GITHUB_RUN_NUMBER}/g"
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: dist
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
